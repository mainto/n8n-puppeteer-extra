# Custom n8n with Puppeteer-Extra

This repo builds a custom `n8n` Docker image with `puppeteer`, `puppeteer-extra`, and `puppeteer-extra-plugin-stealth`.

Image: `ghcr.io/mainto/n8n-puppeteer-custom:latest`
